PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/PlayStack?retryWrites=true&w=majority
JWT_SECRET=change_this_to_a_long_random_secret
# Comma-separated frontend origins (Vercel + local). Change this only — no code edits needed.
CLIENT_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
